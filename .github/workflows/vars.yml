name: Simple Vars Example

on:
  workflow_dispatch

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Show vars and secrets
        run: |
          echo "VAR: ${{ vars.MY_VAR }}"
          echo "SECRET is hidden but exists"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
